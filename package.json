{
  "name": "word-kouyu",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client"
  ],
  "scripts": {
    "android": "cd client && npm run android",
    "build:android": "cd client && npx expo prebuild --platform android && cd android && ./gradlew assembleRelease",
    "install": "cd client && npm install"
  }
}
